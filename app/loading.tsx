import { PageLoading } from "@/components/layout/page-loading";

export default function Loading() {
  return (
    <PageLoading
      title="Loading your next page"
      description="We’re pulling your CRM data and preparing the next page."
    />
  );
}
